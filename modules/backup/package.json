{
  "name": "nexus-backup",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Nexus ecosystem app",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "check": "bunx tsc --noEmit",
    "test": "bun test",
    "lint": "bunx biome check src tests",
    "format": "bunx biome check --write src tests"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "bun-types": "^1.3.11",
    "typescript": "^5.8.3"
  }
}
